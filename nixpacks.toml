[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['npm install']

[phases.build]
cmds = ['chmod +rx node_modules/.bin/tsc', 'node_modules/.bin/tsc']

[start]
cmd = 'node dist/index.js'
